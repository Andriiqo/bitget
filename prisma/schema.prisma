generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = "file:./dev.db"
}

model Ticker {
  id        Int    @id @default(autoincrement()) // Идентификатор пользователя, автоинкремент
  symbol    String
  Exchange  String
  price     String
  timestamp String
}
